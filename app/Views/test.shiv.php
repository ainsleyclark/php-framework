<div style="padding: 100px">
    <h1>{{$test}} hello and ok</h1>

    @foreach ($array as $string)
        {{ $string }} hhh
    @endforeach
    @foreach ($array as $data)
    ggg
    @endforeach
</div>